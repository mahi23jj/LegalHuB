<% layout("layouts/boilerplate") %>

    <style>
        /* ✅ Terms Container */
        .terms-container {
            max-width: 850px;
            margin: 50px auto;
            padding: 40px;
            background-color: rgba(20, 20, 30, 0.9);
            border-radius: 16px;
            box-shadow: 0 12px 24px rgba(0, 0, 0, 0.4);
            line-height: 1.8;
            animation: fadeInUp 0.6s ease;
            border: 1px solid rgba(255, 255, 255, 0.08);
            backdrop-filter: blur(16px);
        }

        /* ✅ Headings */
        h1,
        h2 {
            color: #ff79c6;
            /* Pink Theme */
            margin-bottom: 12px;
            letter-spacing: 0.5px;
        }

        h1 {
            font-size: 2.8rem;
            font-weight: 700;
            text-align: center;
            margin-bottom: 30px;
            border-bottom: 3px solid #ff79c6;
            padding-bottom: 12px;
            transition: color 0.3s ease;
        }

        h1:hover {
            color: #ff6ec7;
        }

        h2 {
            font-size: 1.8rem;
            font-weight: 600;
            margin-top: 30px;
            padding-bottom: 8px;
            border-bottom: 2px solid rgba(255, 255, 255, 0.12);
        }

        /* ✅ Last Updated */
        .last-updated {
            text-align: center;
            font-size: 1rem;
            color: #b2bec3;
            margin-bottom: 32px;
            font-style: italic;
        }

        /* ✅ Paragraphs */
        p {
            font-size: 1rem;
            color: #dcdcdc;
            margin-bottom: 16px;
            line-height: 1.7;
        }

        /* ✅ Strong Text */
        strong {
            color: #ffb86c;
            /* Orange */
            font-weight: 600;
        }

        /* ✅ Custom List Replacement */
        .custom-item {
            margin-bottom: 14px;
            color: #e0e0e0;
            font-size: 1rem;
            padding: 12px 18px;
            border-radius: 8px;
            background-color: rgba(255, 255, 255, 0.05);
            position: relative;
            transition: transform 0.2s ease, background-color 0.2s ease;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
            display: flex;
            align-items: center;
            gap: 12px;
            cursor: pointer;
        }

        .custom-item::before {
            content: "✔️";
            font-size: 20px;
            color: #50fa7b;
            /* Green Checkmark */
            transition: color 0.3s ease;
        }

        .custom-item:hover {
            background-color: rgba(255, 255, 255, 0.08);
            transform: translateY(-3px);
            box-shadow: 0 6px 16px rgba(0, 0, 0, 0.3);
        }

        /* ✅ CTA Button */
        .cta-button {
            display: inline-block;
            margin-top: 24px;
            padding: 14px 28px;
            background-color: #ff79c6;
            color: #ffffff;
            font-size: 1rem;
            font-weight: 600;
            text-decoration: none;
            border-radius: 10px;
            transition: background-color 0.3s ease, transform 0.2s ease;
            box-shadow: 0 6px 16px rgba(255, 121, 198, 0.4);
        }

        .cta-button:hover {
            background-color: #ff6ec7;
            transform: translateY(-4px);
            box-shadow: 0 10px 24px rgba(255, 121, 198, 0.6);
        }

        /* ✅ Footer Styling */
        .footer {
            margin-top: 40px;
            padding: 24px;
            background-color: rgba(20, 20, 30, 0.9);
            text-align: center;
            border-top: 1px solid rgba(255, 255, 255, 0.08);
        }

        .footer-links {
            display: flex;
            justify-content: center;
            gap: 24px;
            flex-wrap: wrap;
        }

        .footer-link {
            color: #ff79c6;
            font-size: 1rem;
            text-decoration: none;
            font-weight: 500;
            transition: color 0.3s ease, transform 0.2s ease;
        }

        .footer-link:hover {
            color: #ff6ec7;
            transform: translateY(-2px);
        }

        .footer-text {
            color: #bbb;
            font-size: 0.9rem;
            margin-top: 12px;
        }

        /* ✅ Animations */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>

    <!-- ✅ Terms and Conditions Content -->
    <section class="terms-container">
        <h1>📜 Terms & Conditions</h1>
        <p class="last-updated">Last updated: <%= new Date().toDateString() %>
        </p>

        <h2>1. Acceptance of Terms</h2>
        <p>
            By accessing or using our platform, you agree to comply with these terms and conditions.
            If you disagree, please do not use our platform.
        </p>

        <h2>2. User Responsibilities</h2>
        <div class="custom-item">Provide accurate information during registration.</div>
        <div class="custom-item">Maintain the confidentiality of your login credentials.</div>
        <div class="custom-item">Avoid using the platform for any illegal activities.</div>

        <h2>3. Intellectual Property</h2>
        <p>
            All content, trademarks, and branding on the platform are the property of <strong>LegalHuB</strong>.
            Unauthorized use, reproduction, or modification is strictly prohibited.
        </p>

        <h2>4. Termination</h2>
        <p>
            We reserve the right to terminate your account without prior notice if you violate these terms.
        </p>

        <h2>5. Contact Us</h2>
        <p>
            If you have any questions, contact us at:
            <strong>📧 support@legalhub.com</strong>
    </section>