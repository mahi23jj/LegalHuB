name: 🏷️ Issue + PR Auto Labeler

on:
  issues:
    types: [opened, edited]
  pull_request:
    types: [opened, edited]

permissions:
  issues: write
  contents: read

jobs:
  autolabel:
    runs-on: ubuntu-latest
    steps:
      - uses: github/issue-labeler@v3.3
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          configuration-path: .github/issue-labeler.yml
          enable-versioned-regex: true
          include-title: 1
          include-body: 1
          sync-labels: 0
